#include <stdio.h>
#include <string.h>

int main(int argc, char **argv)
{
	char month[4];
	int day;
	scanf("%s %d",month,&day);
	if((!strcmp(month,"OCT") && day == 31) || (!strcmp(month,"DEC") && day == 25)){
		printf("yup\n");
	}else{
		printf("nope\n");
	}
	return 0;
}
