#include <stdio.h>

int main(int argc, char **argv)
{
	int n,i;
	scanf("%d",&n);
	int x[n];
	for(i=0;i<n;i++){
		scanf("%d",&x[i]);
	}
	for(i=0;i<n;i++){
		if(x[i]%2 == 0){
			printf("%d is even\n",x[i]);
		}else{
			printf("%d is odd\n",x[i]);
		}
	}
	return 0;
}
