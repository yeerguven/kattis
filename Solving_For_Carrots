#include <stdio.h>

int main(int argc, char **argv)
{
	int a,b,i;
	scanf("%d %d",&a,&b);
	
	if(a<=1000 && b<=1000 && a>=1 && b>=1){
		char c[100];
		for(i=0;i<a+1;i++){
			fgets(c,100,stdin);
		}
		printf("%d\n",b);
	}
	return 0;
}
